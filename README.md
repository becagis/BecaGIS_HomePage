# BecaGIS

## Install plugins

```shell
wp plugin install better-search-replace

wp plugin install \
    redis-cache \
    broken-link-checker \
    regenerate-thumbnails
    really-simple-ssl \
    better-search-replace \
```
